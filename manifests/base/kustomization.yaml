apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sock-shop
  namespace: sock-shop

resources:
  - 00-sock-shop-ns.yaml
  
  - configmaps/
  - secrets/
  #- databases/
  - services/
